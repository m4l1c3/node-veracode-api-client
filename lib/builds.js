'use strict';

const requests = require('request-promise-native'),
    creds = require('/Users/jwisdom/Desktop/vc.json'),
    endpoints = require('../config/endpoints.json');

class Builds {
    
}

module.exports = new Builds();
